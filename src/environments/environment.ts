// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCc7_ZzzzXu6kFlH6tL1U9RmzveOlPRNPc",
    authDomain: "angular-firebase-412e2.firebaseapp.com",
    databaseURL: "https://angular-firebase-412e2.firebaseio.com",
    projectId: "angular-firebase-412e2",
    storageBucket: "angular-firebase-412e2.appspot.com",
    messagingSenderId: "740460513267"
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
